#pragma once


enum CraftableType{
    Stone,
    Iron,
    Wood
};

enum PotionType{
        Health,
        Turn,
        Attack
};

enum WeaponType{
    Bow,
    Axe,
    Sword
};

enum ItemType{
    Wep,
    Pot
};